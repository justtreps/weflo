import { createSectionDefinition } from "./factory";
export const ingredientsSection = createSectionDefinition("ingredients", "Ingrédients & détails", "content", "cards", { title: "Ce qu’il y a dedans" });
