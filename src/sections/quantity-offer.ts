import type { EditorBlock, EditorSection, SettingValue } from "../editor/document";
import type { InspectorControl } from "../editor/section-schema";
import { blockValue, escapeHtml, textControl, value } from "./shared";
import type { BlockDefinition, SectionPackDefinition, SectionVariantDefinition } from "./types";

const MIXED_MESSAGE = "Une application Shopify est requise pour les offres multi-produits.";
const controls: InspectorControl[] = [textControl("title", "Titre"), textControl("subtitle", "Sous-titre"), textControl("text", "Texte", "textarea"), textControl("product_handle", "Produit Shopify"), textControl("quantity_label", "Libellé des quantités"), textControl("cta_label", "Libellé du bouton")];
const tierControls: InspectorControl[] = [textControl("title", "Titre"), textControl("subtitle", "Sous-titre"), textControl("badge", "Badge"), { key: "quantity", label: "Quantité", type: "number", scope: "settings" }, { key: "discount_type", label: "Type de remise", type: "select", scope: "settings", options: ["percentage", "amount", "none"], optionLabels: { percentage: "Pourcentage", amount: "Montant fixe", none: "Aucune remise" } }, { key: "discount_value", label: "Valeur de la remise", type: "number", scope: "settings" }, textControl("product_handle", "Produit Shopify"), textControl("variant_id", "Variante Shopify"), { key: "preselected", label: "Sélectionnée par défaut", type: "toggle", scope: "settings" }, { key: "show_variant_picker", label: "Afficher le choix de variante", type: "toggle", scope: "settings" }];
const tier: BlockDefinition = { type: "offer-tier", name: "Palier d’offre", defaults: { title: "Duo", subtitle: "2 unités", badge: "Le plus choisi", quantity: 2, discount_type: "percentage", discount_value: 10, product_handle: "", variant_id: "", preselected: false, show_variant_picker: false }, settings: tierControls };
const variants: SectionVariantDefinition[] = [
  { id: "horizontal-cards", name: "Cartes horizontales", description: "Paliers côte à côte.", composition: "tiers-en-cartes-horizontales", previewFixtureId: "", defaults: { variant: "horizontal-cards" } },
  { id: "stacked-premium", name: "Paliers premium", description: "Paliers verticaux.", composition: "paliers-premium-empilés", previewFixtureId: "", defaults: { variant: "stacked-premium" } },
  { id: "tier-table", name: "Table de paliers", description: "Lecture structurée.", composition: "table-des-paliers", previewFixtureId: "", defaults: { variant: "tier-table" } },
];
const defaults: Record<string, SettingValue> = { title: "Choisissez votre quantité", subtitle: "Plus vous choisissez, plus vous économisez.", text: "", product_handle: "", quantity_label: "Choisir la quantité", cta_label: "Ajouter au panier", variant: "horizontal-cards" };

function tiers(section: EditorSection): EditorBlock[] { return section.blocks.filter((block) => block.type === "offer-tier" || block.type === "offer"); }
function normalized(value: unknown): string { return typeof value === "string" ? value.trim().toLowerCase() : ""; }
function selectedId(items: EditorBlock[]): string { return items.find((item) => item.settings.preselected === true)?.id ?? items[0]?.id ?? ""; }
function mixed(section: EditorSection, items: EditorBlock[]): boolean { let reference = normalized(section.settings.product_handle); for (const item of items) { const current = normalized(item.settings.product_handle) || reference; if (!current) continue; if (reference && current !== reference) return true; reference = current; } return false; }
function discount(block: EditorBlock): string { const type = blockValue(block, "discount_type", "none"); const amount = blockValue(block, "discount_value"); return !amount || type === "none" ? "" : type === "percentage" ? `-${amount} %` : `-${amount} €`; }
function webTier(block: EditorBlock, active: string, disabled: boolean): string { const quantity = Math.max(1, Number(blockValue(block, "quantity", "1")) || 1); const chosen = block.id === active; const variant = blockValue(block, "variant_id"); const subtitle = blockValue(block, "subtitle", blockValue(block, "text")); return `<label class="wf-quantity-offer__tier${chosen ? " is-selected" : ""}" data-wf-block-id="${escapeHtml(block.id)}"><input type="radio" name="quantity" value="${quantity}"${chosen ? " checked" : ""}${disabled ? " disabled" : ""} data-wf-quantity="${quantity}"${variant ? ` data-wf-variant-id="${escapeHtml(variant)}"` : ""}><span class="wf-quantity-offer__tier-copy"><span class="wf-quantity-offer__tier-title">${escapeHtml(blockValue(block, "title", `${quantity} unités`))}</span>${subtitle ? `<span class="wf-quantity-offer__tier-subtitle">${escapeHtml(subtitle)}</span>` : ""}${block.settings.show_variant_picker === true ? `<span class="wf-quantity-offer__variant">${escapeHtml(variant || "Variante choisie dans Shopify")}</span>` : ""}</span>${blockValue(block, "badge") ? `<span class="wf-quantity-offer__badge">${escapeHtml(blockValue(block, "badge"))}</span>` : ""}${discount(block) ? `<strong class="wf-quantity-offer__discount">${escapeHtml(discount(block))}</strong>` : ""}</label>`; }
function renderWeb(section: EditorSection, pageName: string): string { const requested = value(section, "variant", "horizontal-cards"); const layout = variants.some((item) => item.id === requested) ? requested : "horizontal-cards"; const items = tiers(section); const requiresApp = mixed(section, items); return `<section class="wf-section wf-quantity-offer wf-quantity-offer--${escapeHtml(layout)}" data-wf-variant="${escapeHtml(layout)}"><header>${value(section, "subtitle") ? `<p class="wf-section__eyebrow">${escapeHtml(value(section, "subtitle"))}</p>` : ""}<h2 data-wf-edit-key="title">${escapeHtml(value(section, "title", pageName))}</h2>${value(section, "text") ? `<p class="wf-section__copy">${escapeHtml(value(section, "text"))}</p>` : ""}</header>${requiresApp ? `<aside class="wf-quantity-offer__app-required" role="status">${MIXED_MESSAGE}</aside>` : ""}<form class="wf-product__form wf-quantity-offer__form" action="/cart/add" method="post"><label class="wf-quantity-offer__product">Produit Shopify<select name="id"><option value="">Choisir dans Shopify</option></select></label><fieldset class="wf-quantity-offer__tiers"><legend>${escapeHtml(value(section, "quantity_label", "Choisir la quantité"))}</legend><div class="wf-quantity-offer__tiers-layout">${items.map((item) => webTier(item, selectedId(items), requiresApp)).join("") || "<p>Ajoutez un palier d’offre.</p>"}</div></fieldset><button type="submit"${requiresApp ? " disabled" : ""}>${escapeHtml(value(section, "cta_label", "Ajouter au panier"))}</button></form></section>`; }
function renderLiquid(): string { return `<section class="wf-section wf-quantity-offer wf-quantity-offer--{{ section.settings.variant | escape }}" data-wf-product data-wf-purchase-strategy="multipack" data-wf-section-id="{{ section.id }}"><header><h2>{{ section.settings.title | escape }}</h2></header>{% assign wf_reference_handle = section.settings.product_handle | strip | downcase %}{% assign wf_mixed_product_offer = false %}{% for block in section.blocks %}{% if block.type == 'offer-tier' or block.type == 'offer' %}{% assign wf_tier_handle = block.settings.product_handle | default: wf_reference_handle | strip | downcase %}{% if wf_reference_handle == blank and wf_tier_handle != blank %}{% assign wf_reference_handle = wf_tier_handle %}{% elsif wf_tier_handle != blank and wf_tier_handle != wf_reference_handle %}{% assign wf_mixed_product_offer = true %}{% endif %}{% endif %}{% endfor %}{% assign wf_selected_tier_id = '' %}{% for block in section.blocks %}{% if (block.type == 'offer-tier' or block.type == 'offer') and block.settings.preselected and wf_selected_tier_id == blank %}{% assign wf_selected_tier_id = block.id %}{% endif %}{% endfor %}{% if wf_selected_tier_id == blank %}{% for block in section.blocks %}{% if block.type == 'offer-tier' or block.type == 'offer' %}{% assign wf_selected_tier_id = block.id %}{% break %}{% endif %}{% endfor %}{% endif %}{% assign selected_product = all_products[wf_reference_handle] | default: product %}{% if wf_mixed_product_offer %}<aside class="wf-quantity-offer__app-required" role="status">Une application Shopify est requise pour les offres multi-produits.</aside>{% endif %}{% if selected_product != blank %}{% form 'product', selected_product, class: 'wf-product__form wf-quantity-offer__form' %}<input type="hidden" name="id" value="{{ selected_product.selected_or_first_available_variant.id }}" data-wf-variant-input><fieldset class="wf-quantity-offer__tiers"><legend>{{ section.settings.quantity_label | default: 'Choisir la quantité' | escape }}</legend>{% for block in section.blocks %}{% if block.type == 'offer-tier' or block.type == 'offer' %}{% assign tier_product = all_products[block.settings.product_handle] | default: selected_product %}{% assign tier_variant = tier_product.selected_or_first_available_variant %}{% for variant in tier_product.variants %}{% if variant.id == block.settings.variant_id %}{% assign tier_variant = variant %}{% endif %}{% endfor %}<label class="wf-quantity-offer__tier" data-wf-block-id="{{ block.id }}" {{ block.shopify_attributes }}><input type="radio" name="quantity" value="{{ block.settings.quantity | default: 1 }}" data-wf-quantity="{{ block.settings.quantity | default: 1 }}" data-wf-variant-id="{{ tier_variant.id }}"{% if block.id == wf_selected_tier_id %} checked{% endif %}{% if wf_mixed_product_offer %} disabled{% endif %}><span>{{ block.settings.title | escape }}</span>{% assign subtitle = block.settings.subtitle | default: block.settings.text %}{% if subtitle != blank %}<small>{{ subtitle | escape }}</small>{% endif %}<strong>{{ tier_variant.price | money }}</strong></label>{% endif %}{% endfor %}</fieldset><button type="submit" data-wf-add-to-cart{% if wf_mixed_product_offer or selected_product.selected_or_first_available_variant.available == false %} disabled{% endif %}>{{ section.settings.cta_label | default: 'Ajouter au panier' | escape }}</button>{% endform %}<script type="application/json" data-wf-variants>{{ selected_product.variants | json }}</script><script src="{{ 'weflo-product-form.js' | asset_url }}" defer="defer"></script>{% else %}<p class="wf-product__setup" role="status">Associe un produit Shopify à cette section avant publication.</p>{% endif %}</section>`; }
function migrate(section: EditorSection): EditorSection { const values = value(section, "quantity_breaks", "1,2,3").split(",").map((item) => Number(item.trim())).filter((item) => Number.isInteger(item) && item > 0); let index = 0; const variant = value(section, "variant", "horizontal-cards") === "single-duo-trio" ? "horizontal-cards" : value(section, "variant") === "volume-ladder" ? "stacked-premium" : value(section, "variant", "horizontal-cards"); return { ...section, settings: { ...section.settings, variant }, blocks: section.blocks.map((block) => { const settings = { ...block.settings }; if (block.type !== "offer") return { ...block, settings }; const quantity = typeof settings.quantity === "number" && settings.quantity > 0 ? settings.quantity : values[index] ?? index + 1; index += 1; return { ...block, type: "offer-tier", settings: { ...settings, subtitle: blockValue(block, "subtitle", blockValue(block, "text")), quantity, discount_type: blockValue(block, "discount_type", "none"), discount_value: settings.discount_value ?? 0, product_handle: blockValue(block, "product_handle", value(section, "product_handle")), variant_id: blockValue(block, "variant_id"), preselected: settings.preselected === true, show_variant_picker: settings.show_variant_picker === true } }; }), packVersion: 1, variantId: section.variantId ?? variant }; }
function schema() { return { name: "Offre quantité", settings: controls.map((control) => ({ id: control.key, label: control.label, type: control.type })), blocks: [{ type: tier.type, name: tier.name, settings: tierControls.map((control) => ({ id: control.key, label: control.label, type: control.type })) }], presets: variants.map((variant) => ({ name: variant.name, settings: { ...variant.defaults } })) }; }
export const quantityOfferSection: SectionPackDefinition = { type: "quantity-offer", name: "Offre quantité", category: "commerce", defaults, settings: controls, blocks: [tier], families: ["quantity-offer"], tags: ["quantité", "volume", "économie"], supportedPages: ["landing", "product", "collection", "home"], supportedMarkets: ["all"], capabilities: ["product-form", "quantity-breaks"], variants, assets: [], packVersion: 1, renderWeb: ({ section, pageName }) => renderWeb(section, pageName), renderLiquid: () => renderLiquid(), renderSchema: () => schema(), migrate };
