import type { EditorBlock, EditorSection, SettingValue } from "../editor/document";
import type { InspectorControl } from "../editor/section-schema";
import { blockValue, escapeHtml, textControl, value } from "./shared";
import type { BlockDefinition, SectionPackDefinition, SectionVariantDefinition } from "./types";

const MIXED = "Une application Shopify est requise pour les offres multi-produits.";
const controls: InspectorControl[] = [textControl("title", "Titre"), textControl("subtitle", "Sous-titre"), textControl("text", "Texte", "textarea"), textControl("product_handle", "Produit Shopify"), textControl("quantity_label", "Libellé des quantités"), textControl("cta_label", "Libellé du bouton")];
const tierControls: InspectorControl[] = [textControl("title", "Titre"), textControl("subtitle", "Sous-titre"), textControl("badge", "Badge"), { key: "quantity", label: "Quantité", type: "number", scope: "settings" }, { key: "discount_type", label: "Type de remise", type: "select", scope: "settings", options: ["percentage", "amount", "none"], optionLabels: { percentage: "Pourcentage", amount: "Montant fixe", none: "Aucune remise" } }, { key: "discount_value", label: "Valeur de la remise", type: "number", scope: "settings" }, textControl("product_handle", "Produit Shopify"), textControl("variant_id", "Variante Shopify"), { key: "preselected", label: "Sélectionnée par défaut", type: "toggle", scope: "settings" }, { key: "show_variant_picker", label: "Afficher le choix de variante", type: "toggle", scope: "settings" }];
const tier: BlockDefinition = { type: "offer-tier", name: "Palier d’offre", defaults: { title: "Duo", subtitle: "2 unités", badge: "Le plus choisi", quantity: 2, discount_type: "percentage", discount_value: 10, product_handle: "", variant_id: "", preselected: false, show_variant_picker: false }, settings: tierControls };
const variants: SectionVariantDefinition[] = ["horizontal-cards", "stacked-premium", "tier-table"].map((id) => ({ id, name: id === "horizontal-cards" ? "Cartes horizontales" : id === "stacked-premium" ? "Paliers premium" : "Table de paliers", description: "Composition d’offre quantité.", composition: id, previewFixtureId: "", defaults: { variant: id } }));
const defaults: Record<string, SettingValue> = { title: "Choisissez votre quantité", subtitle: "Plus vous choisissez, plus vous économisez.", text: "", product_handle: "", quantity_label: "Choisir la quantité", cta_label: "Ajouter au panier", variant: "horizontal-cards" };
const offers = (section: EditorSection) => section.blocks.filter((block) => block.type === "offer-tier" || block.type === "offer");
function chosen(items: EditorBlock[]) { return items.find((item) => item.settings.preselected === true)?.id ?? items[0]?.id ?? ""; }
function isMixed(section: EditorSection, items: EditorBlock[]) { let root = String(section.settings.product_handle ?? "").trim().toLowerCase(); for (const item of items) { const current = String(item.settings.product_handle ?? root).trim().toLowerCase(); if (root && current && root !== current) return true; root ||= current; } return false; }
function webTier(block: EditorBlock, active: string, locked: boolean) { const quantity = Math.max(1, Number(blockValue(block, "quantity", "1")) || 1); const variant = blockValue(block, "variant_id"); return `<label data-wf-block-id="${escapeHtml(block.id)}"><input type="radio" name="quantity" value="${quantity}"${block.id === active ? " checked" : ""}${locked ? " disabled" : ""} data-wf-quantity="${quantity}"${variant ? ` data-wf-variant-id="${escapeHtml(variant)}"` : ""}><span>${escapeHtml(blockValue(block, "title", `${quantity} unités`))}</span>${blockValue(block, "subtitle", blockValue(block, "text")) ? `<small>${escapeHtml(blockValue(block, "subtitle", blockValue(block, "text")))}</small>` : ""}${blockValue(block, "badge") ? `<strong>${escapeHtml(blockValue(block, "badge"))}</strong>` : ""}</label>`; }
function renderWeb(section: EditorSection, pageName: string) { const items = offers(section); const locked = isMixed(section, items); const layout = variants.some((item) => item.id === value(section, "variant")) ? value(section, "variant") : "horizontal-cards"; return `<section class="wf-section wf-quantity-offer wf-quantity-offer--${escapeHtml(layout)}"><header>${value(section, "subtitle") ? `<p>${escapeHtml(value(section, "subtitle"))}</p>` : ""}<h2>${escapeHtml(value(section, "title", pageName))}</h2>${value(section, "text") ? `<p>${escapeHtml(value(section, "text"))}</p>` : ""}</header>${locked ? `<aside role="status">${MIXED}</aside>` : ""}<form class="wf-product__form" action="/cart/add"><fieldset><legend>${escapeHtml(value(section, "quantity_label", "Choisir la quantité"))}</legend>${items.map((item) => webTier(item, chosen(items), locked)).join("")}</fieldset><button type="submit"${locked ? " disabled" : ""}>${escapeHtml(value(section, "cta_label", "Ajouter au panier"))}</button></form></section>`; }
function renderLiquid() { return String.raw`{% assign wf_reference_handle = section.settings.product_handle | strip | downcase %}{% assign wf_mixed_product_offer = false %}{% assign wf_selected_tier_id = '' %}{% for block in section.blocks %}{% if block.type == 'offer-tier' or block.type == 'offer' %}{% assign wf_tier_handle = block.settings.product_handle | default: wf_reference_handle | strip | downcase %}{% if wf_reference_handle == blank %}{% assign wf_reference_handle = wf_tier_handle %}{% elsif wf_tier_handle != blank and wf_tier_handle != wf_reference_handle %}{% assign wf_mixed_product_offer = true %}{% endif %}{% if block.settings.preselected %}{% if wf_selected_tier_id == blank %}{% assign wf_selected_tier_id = block.id %}{% endif %}{% endif %}{% endif %}{% endfor %}{% if wf_selected_tier_id == blank %}{% for block in section.blocks %}{% if block.type == 'offer-tier' or block.type == 'offer' %}{% assign wf_selected_tier_id = block.id %}{% break %}{% endif %}{% endfor %}{% endif %}<section class="wf-section wf-quantity-offer wf-quantity-offer--{{ section.settings.variant | escape }}" data-wf-product data-wf-purchase-strategy="multipack" data-wf-section-id="{{ section.id }}"><header>{% if section.settings.subtitle != blank %}<p>{{ section.settings.subtitle | escape }}</p>{% endif %}<h2>{{ section.settings.title | escape }}</h2>{% if section.settings.text != blank %}<p>{{ section.settings.text | escape }}</p>{% endif %}</header>{% assign selected_product = all_products[wf_reference_handle] | default: product %}{% if wf_mixed_product_offer %}<aside role="status">Une application Shopify est requise pour les offres multi-produits.</aside>{% endif %}{% if selected_product != blank %}{% form 'product', selected_product, class: 'wf-product__form' %}<input type="hidden" name="id" value="{{ selected_product.selected_or_first_available_variant.id }}" data-wf-variant-input><fieldset><legend>{{ section.settings.quantity_label | default: 'Choisir la quantité' | escape }}</legend>{% for block in section.blocks %}{% if block.type == 'offer-tier' or block.type == 'offer' %}<label data-wf-block-id="{{ block.id }}" {{ block.shopify_attributes }}><input type="radio" name="quantity" value="{{ block.settings.quantity | default: 1 }}" data-wf-quantity="{{ block.settings.quantity | default: 1 }}" data-wf-variant-id="{{ block.settings.variant_id | default: selected_product.selected_or_first_available_variant.id }}"{% if block.id == wf_selected_tier_id %} checked{% endif %}{% if wf_mixed_product_offer %} disabled{% endif %}><span>{{ block.settings.title | escape }}</span></label>{% endif %}{% endfor %}</fieldset><button type="submit" data-wf-add-to-cart{% if wf_mixed_product_offer or selected_product.selected_or_first_available_variant.available == false %} disabled{% endif %}>{{ section.settings.cta_label | default: 'Ajouter au panier' | escape }}</button>{% endform %}<script src="{{ 'weflo-product-form.js' | asset_url }}" defer="defer"></script>{% endif %}</section>`; }
function migrate(section: EditorSection): EditorSection { const quantities = value(section, "quantity_breaks", "1,2,3").split(",").map(Number); let index = 0; const variant = value(section, "variant") === "single-duo-trio" ? "horizontal-cards" : value(section, "variant") === "volume-ladder" ? "stacked-premium" : value(section, "variant", "horizontal-cards"); return { ...section, settings: { ...section.settings, variant }, blocks: section.blocks.map((block) => block.type !== "offer" ? { ...block, settings: { ...block.settings } } : { ...block, type: "offer-tier", settings: { ...block.settings, subtitle: blockValue(block, "subtitle", blockValue(block, "text")), quantity: Number(block.settings.quantity) || quantities[index++] || 1, discount_type: blockValue(block, "discount_type", "none"), discount_value: block.settings.discount_value ?? 0, product_handle: blockValue(block, "product_handle", value(section, "product_handle")), variant_id: blockValue(block, "variant_id"), preselected: block.settings.preselected === true, show_variant_picker: block.settings.show_variant_picker === true } }), packVersion: 1, variantId: section.variantId ?? variant }; }
export const quantityOfferSection: SectionPackDefinition = { type: "quantity-offer", name: "Offre quantité", category: "commerce", defaults, settings: controls, blocks: [tier], families: ["quantity-offer"], tags: ["quantité", "volume", "économie"], supportedPages: ["landing", "product", "collection", "home"], supportedMarkets: ["all"], capabilities: ["product-form", "quantity-breaks"], variants, assets: [], packVersion: 1, renderWeb: ({ section, pageName }) => renderWeb(section, pageName), renderLiquid, renderSchema: () => ({ name: "Offre quantité", settings: controls.map((control) => ({ id: control.key, label: control.label, type: control.type })), blocks: [{ type: tier.type, name: tier.name, settings: tierControls.map((control) => ({ id: control.key, label: control.label, type: control.type })) }], presets: variants.map((variant) => ({ name: variant.name, settings: variant.defaults })) }), migrate };
